/**
 * @file Declares Topic data type representing relationship between
 * Tuits and tags, as in a tuit can be a topic
 * @property {String} topic a type of tuit
 */
export default class Topic {
    private topic: String | null = null;
}