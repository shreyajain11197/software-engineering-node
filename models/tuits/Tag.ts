/**
 * @file Declares Tag data type representing relationship between
 * Tuits and tags, as in a tuit can be a tag
 * @property {String} tag a type of tuit
 */
export default class Tag {
    private tag: String | null = null;
}